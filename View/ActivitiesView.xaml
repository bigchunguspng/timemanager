<Page x:Class="TimeManager.View.ActivitiesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:TimeManager.ViewModel"
      mc:Ignorable="d"
      Title="Regular activities">

    <Page.DataContext>
        <vm:ActivitiesViewModel />
    </Page.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding PanelWidth}" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="5,0,0,0">
            <ListView
                BorderThickness="1,1,1,0"
                ItemsSource="{Binding SelectedActivityDates}"
                SelectedIndex="{Binding IndexOfSelectedDate}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <TextBlock Text="{Binding}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Remove date" Command="{Binding RemoveDate}" />
                    </ContextMenu>
                </ListView.ContextMenu>
            </ListView>
            <TreeView BorderBrush="#abadb3">
                <TreeViewItem Header="{Binding Analytics[0]}" IsExpanded="True">
                    <TreeViewItem Header="{Binding Analytics[1]}" />
                    <TreeViewItem Header="{Binding Analytics[2]}" />
                    <TreeViewItem Header="{Binding Analytics[3]}" />
                </TreeViewItem>
                <TreeViewItem Header="{Binding Analytics[4]}" IsExpanded="True">
                    <TreeViewItem Header="{Binding Analytics[5]}" />
                    <TreeViewItem Header="{Binding Analytics[6]}" />
                    <TreeViewItem Header="{Binding Analytics[7]}" />
                </TreeViewItem>
            </TreeView>
            <!--<TextBlock Text="{Binding SelectedRoutine.Info}"></TextBlock>-->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <DatePicker
                        SelectedDate="{Binding NewDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Width="95"
                        Margin="0,6,0,0"
                        BorderThickness="0" />
                    <Button
                        Width="35" Margin="0,5,5,0"
                        Content="Add" Command="{Binding AddDate}" />
                </StackPanel>
            </Grid>
        </StackPanel>

        <ListBox
            Grid.Column="1"
            BorderThickness="0"
            ItemsSource="{Binding Activities}"
            SelectedItem="{Binding SelectedActivity}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <TextBox
                                Width="200" TextWrapping="Wrap" BorderThickness="0"
                                Text="{Binding Description}" />
                            <TextBlock
                                Margin="5,0,0,0"
                                Text="{Binding LastTimeInfo}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="New" Command="{Binding NewActivity}" />
                    <MenuItem Header="Remove" Command="{Binding RemoveActivity}" />
                </ContextMenu>
            </ListBox.ContextMenu>
        </ListBox>

    </Grid>
</Page>