<Page x:Class="TimeManager.View.CategoryView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:TimeManager.ViewModel"
      mc:Ignorable="d"
      Title="Category">

    <Page.DataContext>
        <vm:CategoryViewModel />
    </Page.DataContext>

    <Grid>
        <ListView
            ItemsSource="{Binding SelectedCategory.TaskLists}"
            SelectedItem="{Binding SelectedCategory.SelectedTaskList, Mode=TwoWay}"
            BorderThickness="0"
            ScrollViewer.VerticalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <StackPanel Orientation="Horizontal">
                            <TextBox
                                Text="{Binding Name}"
                                Width="275" HorizontalAlignment="Left" Margin="1,0,0,0"
                                BorderThickness="0" Background="Transparent"
                                TextWrapping="Wrap" />
                            <TextBlock
                                Text="{Binding TasksInfo}"
                                TextAlignment="Right"
                                MinWidth="96"
                                Margin="1,0,0,0"
                                Background="Transparent" />
                        </StackPanel>
                        <ListView
                            ItemsSource="{Binding Tasks}"
                            SelectedItem="{Binding SelectedTask}"
                            Visibility="{Binding TasksVisibility, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            BorderThickness="0" Background="Transparent">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="20" />
                                            <ColumnDefinition Width="250" />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>
                                        <Button
                                            Content="{Binding ButtonContent, UpdateSourceTrigger=PropertyChanged}"
                                            Width="20"
                                            Command="{Binding ChangeTaskStatus}" />
                                        <TextBox
                                            Grid.Column="1"
                                            Text="{Binding Description}"
                                            TextWrapping="Wrap" />
                                        <TextBlock
                                            Grid.Column="2"
                                            Text="{Binding TimeInfo}"
                                            MinWidth="40"
                                            Margin="1,1,0,0"
                                            ToolTip="{Binding ToolTipText}" />
                                        <Grid.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="Clear" Command="{Binding ClearTask}" />
                                                <MenuItem Header="Set deadline">
                                                    <StackPanel Orientation="Horizontal" Width="Auto">
                                                        <DatePicker
                                                            SelectedDate="{Binding NewDeadline}"
                                                            BorderThickness="0" />
                                                        <Button
                                                            Content="Set"
                                                            Command="{Binding SetDeadline}"
                                                            Width="45" />
                                                    </StackPanel>
                                                </MenuItem>
                                                <MenuItem Header="Clear deadline" Command="{Binding ClearDeadline}" />
                                            </ContextMenu>
                                        </Grid.ContextMenu>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.InputBindings>
                                <KeyBinding
                                    Key="Delete" Command="{Binding RemoveTask}" />
                            </ListView.InputBindings>
                        </ListView>
                        <StackPanel Orientation="Horizontal" Visibility="{Binding TasksVisibility}">
                            <TextBox
                                Text="{Binding NewTaskDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                TextWrapping="Wrap"
                                HorizontalAlignment="Left"
                                Margin="26,1,0,0"
                                Width="250">
                                <TextBox.InputBindings>
                                    <KeyBinding
                                        Key="Enter" Command="{Binding AddTask}" />
                                </TextBox.InputBindings>
                            </TextBox>
                            <DatePicker
                                SelectedDate="{Binding NewTaskDeadline, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Width="95"
                                Margin="3,1,0,0"
                                BorderThickness="0" />
                        </StackPanel>
                        <StackPanel.InputBindings>
                            <MouseBinding
                                Command="{Binding ChangeTasksVisibility}" MouseAction="MiddleClick" />
                        </StackPanel.InputBindings>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="New list" Command="{Binding NewList }" />
                    <MenuItem Header="Remove" Command="{Binding RemoveList}" />
                </ContextMenu>
            </ListView.ContextMenu>
            <ListView.InputBindings>
                <!--<KeyBinding
                    Command="{Binding MoveUp}"
                    Modifiers="Alt" Key="Up" />
                <KeyBinding
                    Command="{Binding MoveDown}"
                    Modifiers="Alt" Key="Down" />-->
                <KeyBinding
                    Command="{Binding MoveUp}"
                    Modifiers="Alt" Key="Q" />
                <KeyBinding
                    Command="{Binding MoveDown}"
                    Modifiers="Alt" Key="A" />
            </ListView.InputBindings>
        </ListView>
    </Grid>
</Page>