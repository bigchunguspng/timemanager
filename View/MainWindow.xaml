<Window x:Class="TimeManager.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TimeManager.ViewModel"
        mc:Ignorable="d"
        Title="TimeManager"
        CacheMode="BitmapCache"
        WindowStartupLocation="Manual"
        Left="{Binding Left, Mode=TwoWay}"
        Top="{Binding Top, Mode=TwoWay}"
        Height="{Binding Height, Mode=TwoWay}"
        Width="{Binding Width, Mode=TwoWay}">

    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <DockPanel LastChildFill="True">

        <StatusBar DockPanel.Dock="Bottom" Height="28" BorderBrush="#abadb3" BorderThickness="0,1,0,0">
            <StatusBarItem Padding="0,0,6,0">
                <Button
                    Content="Save All" Width="60" Height="27" BorderThickness="0"
                    Command="{Binding SaveAll}" />
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding StatusBarMessenger.Message}" />
            </StatusBarItem>
        </StatusBar>

        <!--<Border DockPanel.Dock="Bottom" BorderBrush="#abadb3" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal">
                <Button
                    Content="Save All" Width="60" Height="25" BorderThickness="0"
                    Command="{Binding SaveAll}">
                </Button>
                <TextBlock Text="Hint "></TextBlock>
            </StackPanel>
        </Border>-->

        <Grid DockPanel.Dock="Left" Width="200">
            <DockPanel>
                <ListBox DockPanel.Dock="Bottom" BorderThickness="0,0,1,0"
                         ItemsSource="{Binding ExtraPages}"
                         SelectedItem="{Binding SelectedPage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Bottom">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock Text="{Binding Title}" FontSize="18" Margin="0, 5,0,5" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <ListBox
                    BorderThickness="0,1,1,1"
                    ItemsSource="{Binding Path=Categories}"
                    SelectedItem="{Binding Path=SelectedCategory, Mode=TwoWay}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <!--<TextBlock 
                            Text="{Binding Name}" 
                            FontSize="18" 
                            Margin="0, 5,0,5" >
                        </TextBlock>-->
                                <TextBox
                                    Text="{Binding Name}"
                                    FontSize="18"
                                    Margin="0, 5,0,5"
                                    BorderThickness="0"
                                    Background="Transparent" />
                                <Ellipse
                                    Grid.Column="1"
                                    Fill="SlateGray"
                                    Width="{Binding IndicatorSize}"
                                    Height="{Binding IndicatorSize}"
                                    Margin="{Binding IndicatorMargin}" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="New category" Command="{Binding NewCategory}" />
                            <MenuItem Header="Remove" Command="{Binding RemoveCategory}" />
                            <MenuItem Header="Restore removed" Command="{Binding RestoreAll}" />
                        </ContextMenu>
                    </ListBox.ContextMenu>
                </ListBox>
            </DockPanel>
        </Grid>

        <Frame
            NavigationUIVisibility="Hidden"
            Content="{Binding SelectedPage}" />

        <DockPanel.InputBindings>
            <KeyBinding
                Modifiers="Control" Key="S"
                Command="{Binding SaveAll}" />
        </DockPanel.InputBindings>

    </DockPanel>

    <!--<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ListBox
            ItemsSource="{Binding Path=Categories}"
            SelectedItem="{Binding Path=SelectedCategory, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        ~1~<TextBlock 
                            Text="{Binding Name}" 
                            FontSize="18" 
                            Margin="0, 5,0,5" >
                        </TextBlock>@1@
                        <TextBox
                            Text="{Binding Name}"
                            FontSize="18"
                            Margin="0, 5,0,5"
                            BorderThickness="0"
                            Background="Transparent" />
                        <Ellipse
                            Grid.Column="1"
                            Fill="SlateGray"
                            Width="{Binding IndicatorSize}"
                            Height="{Binding IndicatorSize}"
                            Margin="{Binding IndicatorMargin}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="New category" Command="{Binding NewCategory}" />
                    <MenuItem Header="Remove" Command="{Binding RemoveCategory}" />
                </ContextMenu>
            </ListBox.ContextMenu>
        </ListBox>
        <ListBox
            Grid.Column="0"
            ItemsSource="{Binding ExtraPages}"
            SelectedItem="{Binding SelectedPage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            VerticalAlignment="Bottom"
            Margin="0,0,0,35">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock Text="{Binding Title}" FontSize="18" Margin="0, 5,0,5" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Frame
            Grid.Column="1"
            Content="{Binding SelectedPage}"
            NavigationUIVisibility="Hidden" />

        <Button Grid.Column="0"
                Width="50" Height="25"
                HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="5"
                Command="{Binding SaveAll}">
            Save All
        </Button>
    </Grid>-->
</Window>